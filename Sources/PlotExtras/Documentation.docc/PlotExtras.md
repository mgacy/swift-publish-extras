# ``PlotExtras``

Utilities for writing HTML using the Publish package.

@Metadata {
    @PageImage(
        purpose: icon,
        source: "logo",
        alt: "An icon representing Mobelux."
    )
    @PageColor(gray)
    @Available(macOS, introduced: "13.0")
}

## Overview

The `PlotExtras` package provides additional methods and types for writing HTML using the [Plot](https://github.com/JohnSundell/Plot) package.

## Topics

### Essentials
